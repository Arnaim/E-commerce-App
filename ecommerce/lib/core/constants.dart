import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFFE91E63); // Pink for cosmetics
  static const Color secondary = Color(0xFFF06292);
  static const Color background = Colors.white;
  static const Color surface = Colors.white;
  static const Color error = Color(0xFFB00020);
}

class PromoConfig {
  static const String title = 'Summer Glow Sale';
  static const String subtitle = 'Up to 40% OFF on Skincare';
  static const String buttonText = 'Shop Now';
  static const List<Color> gradientColors = [Color(0xFFFCE4EC), Color(0xFFF8BBD0)];
}
